# pkgbuild intermediate output (rebuilt by build.sh)
build/

# auto-generated by build.sh
payload/usr/local/mhealth/VERSION

# Python bytecode cache (from `python3 -c "py_compile..."` syntax checks)
__pycache__/
*.pyc

# macOS clutter
.DS_Store

# editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Optional: keep dist/ — the built .pkg lives here (~140 KB), tracked so
# teammates can grab the latest release directly from the repo.
